{% extends 'base.html.twig' %}

{% block body %}
    <h1>Planen</h1>
    {{ form_start(edit_form) }}
    <div class='col-sm-6'>
        {{ form_widget(edit_form) }}
        </br>
        <input id="submitter" type="submit" class="btn btn-default" value="Speichern"/>
        <a href="{{ path('plan_show') }}" class="btn btn-default">Zurück</a>
    </div>
    {{ form_end(edit_form) }}
{% endblock %}

{% block stylesheets %}

{% endblock %}
{% block javascripts %}
    <script src="{{ asset('Public/fullcalendar/de.js') }}"></script>
    <script>
        $(function () {
            $('#plan_plane_date_from').datetimepicker({
                locale: 'de',
                defaultDate: $('#plan_plane_date_to').val()
            });
            $('#plan_plane_date_to').datetimepicker({
                locale: 'de',
                defaultDate: $('#plan_plane_date_to').val()
            });
        });
    </script>
{% endblock %}